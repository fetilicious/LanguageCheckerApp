import styled from 'styled-components';
import Section from './Section';

const CharacterCountAreaErrors = styled(Section)`
  text-align: right;
  margin: 0.5em;
  color: red;
`;

export default CharacterCountAreaErrors;
